{
  "name": "mini-translate",
  "version": "0.1.0",
  "description": "Mini Translate Chrome 扩展的代码与测试工程",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --environment jsdom",
    "test:watch": "vitest --environment jsdom"
  },
  "devDependencies": {
    "jsdom": "^27.0.0",
    "vitest": "^1.5.0"
  }
}
